#include <stdio.h>
int main(){
    float m1 = 39;
    float m2 = 27;
    float m3 = 33;
    float m4 = 40;

    float p =  (m1 + m2 + m3 + m4) * 100 / 160;
    printf("Percentage of 4 subjects mark out of 40 is : %f",p);
    return 0;
}